#include "monty.h"

/**
 *nop - doesn't do anything
 *@stack: head pointer
 *@line_number: node number
 *Return: nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
